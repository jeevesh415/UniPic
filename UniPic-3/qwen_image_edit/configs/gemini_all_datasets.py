from qwen_image_edit.dataset import Unipic3EditDataset, ConcatDataset
import os

# Data paths can be configured via environment variables
# Example: export DATA_ROOT=/path/to/data
DATA_ROOT = os.environ.get('DATA_ROOT', 'data')

dataset = dict(
    type=ConcatDataset,
    datasets=[
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'unipic3_2imgs_quality_v2_filtered.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'unipic3_3imgs_quality_v2_filtered.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'seedream_4imgs_all.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'seedream_5imgs_all.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'seedream_6imgs_all.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'mico_merged.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'nano_merged.jsonl'),
             image_folder=DATA_ROOT,
             image_size=1024,
             unit_image_size=32,
             ),
        dict(type=Unipic3EditDataset,
             data_path=os.path.join(DATA_ROOT, 'sft_processed_trainset.jsonl'),
             image_folder='',
             image_size=1024,
             unit_image_size=32,
             ),
    ],
)
